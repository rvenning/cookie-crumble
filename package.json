{
  "name": "cookie-crumble",
  "version": "1.0.0",
  "private": true,
  "description": "Run a cookie shop: mix, cut, bake it just right, ice it and sell it — a bright-kitchen game built on gamekit.",
  "scripts": {
    "test": "node --test",
    "report": "cross-env-unavailable-use: CC_REPORT=1 node --test tests/bot.test.js",
    "icons": "node tools/make-icons.js"
  }
}
